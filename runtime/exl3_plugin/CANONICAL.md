# Canonical home moved

The EXL3 plugin now lives at **https://github.com/vcruz305/vllm-exl3**
(package `vllm_exl3`, Apache-2.0). This in-repo copy is retained for
provenance; new development, issues, and releases happen there. The
`glm53_exl3_plugin` import path keeps working via a compatibility shim.
